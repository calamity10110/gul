# GUL v3.1 Web App Template

@imp std.http
@imp std.ui

async fetch_users():
    res = await http.get("https://api.example.com/users")
    return res.json()

let user_list = ^÷^[table{
    headers=["ID", "Name", "Email"],
    rows=[]
}]

mn:
    users = await fetch_users()
    ui.print(user_list)
    print("Web app running!")
