# GUL v3.1 Basic Project Template

@imp std.ui

let tree = ^÷^[tree]
let slider = ^÷^[slider{min=0, max=100, value=50}]

mn:
    ui.print(tree)
    ui.print(slider)
    print("Basic project initialized!")
