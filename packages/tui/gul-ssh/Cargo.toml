[package]
name = "gul-ssh"
version = "0.1.0"
edition = "2021"
authors = ["GUL Team <team@gul-lang.org>"]
description = "SSH client and server for GUL"
license = "MIT"
repository = "https://github.com/gul-lang/packages"
keywords = ["ssh", "client", "server", "gul"]
categories = ["network-programming"]

[dependencies]
tokio = { version = "1.35", features = ["full"] }

[dev-dependencies]
tokio-test = "0.4"
