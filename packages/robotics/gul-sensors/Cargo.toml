[package]
name = "gul-sensors"
version = "0.1.0"
edition = "2021"

[features]
default = ["stub"]
stub = []
real = []          # Placeholder for sensor crates (mpu6050, etc)

[dependencies]
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
