[package]
name = "gul-ml"
version = "0.1.0"
edition = "2021"
authors = ["GUL Team <team@gul-lang.org>"]
description = "Machine Learning framework for GUL - model creation, training, distillation, and inference"
license = "MIT"
repository = "https://github.com/gul-lang/packages"
keywords = ["ml", "machine-learning", "ai", "neural-network", "gul"]
categories = ["science"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
