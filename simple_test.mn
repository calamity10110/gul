# GUL Simple Test - v3.0
# Basic syntax demonstration

struct Point:
    x: int
    y: int

let msg = "Hello from GUL Interpreter!"
data = [1, 2, 3]
data2 = (1, 2, 3)  # Also creates a list
data3 = {1, 2, 3}  # Also creates a list

# v3.0: Dict with any brackets
config = [host: "localhost", port: 8080]
# v3.0: main block - just mn:
mn:
    print(msg)
    print(10 + 20)

# v3.0: Bracket equivalence for collections


print("Done!")