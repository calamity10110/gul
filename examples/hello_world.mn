# GUL Hello World - v3.0
# Demonstrates basic syntax with v3.0 features

mn:
    let message: str = "Hello, World!"
    var count: int = 42
    print(message)
    print("Count:", count)

    # v3.0: Bracket equivalence - all valid
    print["Using square brackets"]
    print{"Using curly braces"}

    # Test error handling
    try:
        print("Testing try/catch...")
    catch error:
        print("Caught error:", error)
