main():
    const message: str = "Hello, World!"
    mut count: int = 42
    print(message)
    print("Count:", count)

    # Test error handling
    try:
        print("Testing try/catch...")
    catch error:
        print("Caught error:", error)
