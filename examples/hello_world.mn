# GUL Hello World - v3.2

@imp std.io

@fn greet(name)(result):
    result = "Hello, " + name

mn:
    @const message = "Hello, World!"
    @var count = 42
    
    println(message)
    println("Count: " + @str(count))
    
    @const greeting = greet("GUL v3.2")
    println(greeting)
    
    println("✓ Hello World complete!")
