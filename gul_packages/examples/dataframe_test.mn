# GUL DataFrame Test
# Tests basic DataFrame functionality

@imp std.data

mn:
    print("=== DataFrame Test ===")
    
    # Create manual data
    var dataset = {
        "Name": ["Alice", "Bob", "Charlie"],
        "Age": [25, 30, 35],
        "Score": [88.5, 92.0, 79.5]
    }
    
    # Create DataFrame
    print("Creating DataFrame from dict...")
    var df = DataFrame::from_dict(dataset)
    
    # Show summary
    df.describe()
    
    # Show head
    df.head(5)
    
    print("=== Test Complete ===")
