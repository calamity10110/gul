# Test dict storage and keys
let test_dict = @dict{
    "fn": "keyword1",
    "let": "keyword2"
}

print("Test dict contents:")
print(test_dict)
print("Keys:")
# Can we iterate?
# Try direct key check
let has_fn = "fn" in test_dict
print("fn check result: " + str(has_fn))

mn:
    print("Done!")
